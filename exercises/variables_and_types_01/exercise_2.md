Exercise 2 - Prime Numbers (Loop + Conditional)

    ! EXAMPLES !

        public class PrimeNumbers {
            public static void main(String[] args) {
                int limit = 30;
        
                // Print all prime numbers up to 'limit'
                // Hint: prime number is divisible only by 1 and itself
                // Use nested loops if needed
            }
        }